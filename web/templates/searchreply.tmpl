{{define "content"}}
  <div class="container">
    <div class="row">
      <div class="col">
        {{block "searchform" .}}{{end}}
      </div>
    </div>
    <div class="row">
      {{block "pets" .}}{{end}}
    </div>
  </div>
{{end}}
